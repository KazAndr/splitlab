# SPLIT — **S**imple **P**ulse **L**abeling **I**n**T**erface

Python package name: `splitlab`  
Repository suggestion: `splitlab`  
Created: 2026-02-09

## Quick start (pyenv)

```bash
./scripts/bootstrap_pyenv.sh
python -m splitlab
```

## Project layout
- `src/splitlab/` — application package
- `src/splitlab/core/` — data/model layer
- `src/splitlab/ui/` — Qt widgets & window composition
- `scripts/` — helper scripts
- `legacy/` — old GUI code (kept for history)

